export interface ButtonDescriptor {
  id: string;
  label: string;
  x: number;
  y: number;
  w: number;
  h: number;
  enabled: boolean;
}

export const CARD_WIDTH = 128;
export const CARD_HEIGHT = 178;
